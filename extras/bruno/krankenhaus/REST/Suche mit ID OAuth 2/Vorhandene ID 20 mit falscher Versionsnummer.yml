info:
  name: Vorhandene ID 20 mit falscher Versionsnummer
  type: http
  seq: 3

http:
  method: GET
  url: "{{restUrl}}/:id"
  headers:
    - name: Accept
      value: application/json
    - name: If-None-Match
      value: xxx
  params:
    - name: id
      value: "20"
      type: path
  auth: inherit

runtime:
  assertions:
    - expression: res.status
      operator: eq
      value: "200"
    - expression: res.headers['content-type']
      operator: eq
      value: application/json
    - expression: res.body.id
      operator: eq
      value: "20"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
