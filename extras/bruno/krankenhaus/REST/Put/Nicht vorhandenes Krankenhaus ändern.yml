info:
  name: Nicht vorhandenes Krankenhaus ändern
  type: http
  seq: 4

http:
  method: PUT
  url: "{{restUrl}}/:id"
  headers:
    - name: If-Match
      value: '"0"'
  params:
    - name: id
      value: "1"
      type: path
  body:
    type: json
    data: |-
      {
          "name": "Braunwald Klinik",
          "mitarbeiteranzahl": 67,
          "bettenanzahl": 420,
          "email": "braunwald@klinik.de"
      }
  auth: inherit

runtime:
  assertions:
    - expression: res.status
      operator: eq
      value: "404"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
